export default function AdminPlansPage() {
    return (
        <div className="p-8">
            <h1 className="text-2xl font-bold text-text-heading">Recurring Plans</h1>
            <p className="mt-2 text-text-muted">Configure billing cycles and pricing rules.</p>
        </div>
    );
}
